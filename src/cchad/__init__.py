"""cchad — a package manager and lockfile for your Claude Code setup.

Scan a repo, detect its stack, and install a curated, deconflicted set of MCP
servers and skills — recording every choice in a committed manifest so the whole
team gets an identical Claude Code environment with one command.
"""

__version__ = "0.1.1"
