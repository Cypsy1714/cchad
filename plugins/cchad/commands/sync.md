---
description: Rebuild this repo's Claude Code config from its committed manifest.
---
This repo has a committed `auto_plugins_mcps.md`. Reproduce its setup locally.

1. Run `uvx cchad sync --yes` in the current working directory with the Bash tool.
2. Report what was written and surface any manual steps.
3. Tell the user to reload Claude Code so the synced MCP servers and skills load.
