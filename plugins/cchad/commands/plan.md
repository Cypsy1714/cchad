---
description: Preview what cchad would configure for this repo. Changes nothing.
---
Run `uvx cchad plan` in the current working directory with the Bash tool and show
the user the resulting plan: the packages cchad would add (with scope and reason)
and the ones it would deliberately skip (with reason).

This is a preview only — do not apply anything. Mention they can run `/cchad:init`
to apply the plan.
